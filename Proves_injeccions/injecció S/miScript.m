% Archivo: miScript.m

% Llamar a la función y almacenar el vector en la variable 'miVector'
miVector = miFuncion();

% Mostrar el vector
disp('El vector retornado es:');
disp(miVector);

% Usar el vector para algún cálculo
sumaVector = sum(miVector);
disp('La suma de los elementos del vector es:');
disp(sumaVector);
